{
  "name": "yayjaybae.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "npm run build && cp -r cloudflare/* out/ && npx wrangler pages publish out --project-name www-yayjaybae-com-pages --branch main",
    "deploy:windows": "npm run build && xcopy cloudflare\\* out\\ /E /I /Y && npx wrangler pages publish out --project-name www-yayjaybae-com-pages --branch main"
  },
  "dependencies": {
    "motion": "^11.15.0",
    "next": "15.1.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
